/*
function fib(n) {
    // Base cases
    if (n === 0) return 0;
    if (n === 1) return 1;

    // Recursive case
    console.log("Inside fib function n is",n);
    return fib(n - 1) + fib(n - 2);
}

console.log(fib(45))

function add(a,b){
    return a+b;
}

function sub(a,b){
    return a-b;
}

function multiply(a,b){
    return a*b;
}
function divide(a,b){
    return a/b;
}

let valueA=Math.floor(Math.random()*10);
let valueB=1;
let operation= add;
console.log(operation(valueA,valueB));

*/


let numArray = [22,34,2,45,22,42,57,90,98,62,41,75,82,43,12,37];

/*function printToConsole(arg){
    console.log(arg);
}

printToConsole(...numArray);

let printToConsole = function(arg){
    console.log(arg);
}

printToConsole(numArray);

let printToConsole = {
    print: function(arg){
        console.log("You'r in an object method",arg);
    }, 
    popARray: function(arg){
        console.log(arg.pop());
    }
}

printToConsole.print(numArray);
printToConsole.popARray(numArray);
printToConsole.print(numArray);
printToConsole.popARray(numArray);
printToConsole.print(numArray);


(function(arg){
    console.log("tricky function:",arg);
})
(numArray);
console.log("\n\n");
*/